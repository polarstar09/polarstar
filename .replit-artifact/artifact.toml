kind = "web"
previewPath = "/"
title = "Polarstar Sanitary Store"
version = "1.0.0"
id = "artifacts/polarstar"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25553

[services.development]
run = "pnpm --filter @workspace/polarstar run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/polarstar", "run", "build" ]
publicDir = "artifacts/polarstar/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25553"
BASE_PATH = "/"
